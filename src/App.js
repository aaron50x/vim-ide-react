import React, { Component } from "react";

class App extends Component {

    constructor(props) {
        super(props);
    }

    render() {
        return <div>Template App!!!, this includes the bash script <code>./start-ide</code> to start your own vim based IDE</div>;
    }
}

export default App;
